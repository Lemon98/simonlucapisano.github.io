function setup() {
    createCanvas(windowWidth, windowHeight);
    fullscreen();
}
  
  function draw() {
    noFill();
    strokeWeight(4);
    stroke(255);
    ellipse(width/2, height/2, 200, 200);
    
  }